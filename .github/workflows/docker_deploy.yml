name: Deploy buy runner
on:
  push:
    branches: [ "stage" ]
jobs:
  prepare-env:
    runs-on: self-hosted
    steps:
      - name: git
        run:  cd /home/vd/Trimiata/Trimiata_test/test_cicd/ && git fetch && git pull
      - name: docker down
        run: cd /home/vd/Trimiata/Trimiata_test/test_cicd/docker/ && docker compose down
      - name: docker build
        run: cd /home/vd/Trimiata/Trimiata_test/test_cicd/docker/ &&docker compose up --build -d
        
